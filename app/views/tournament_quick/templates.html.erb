<div class="min-h-screen bg-gray-100">
  <!-- 헤더 -->
  <div class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="py-6">
        <h1 class="text-3xl font-bold text-gray-900">
          🏀 간편 대회 만들기
        </h1>
        <p class="mt-2 text-gray-600">
          5분 만에 대회를 만들어보세요! 복잡한 설정은 BDR이 대신 해드립니다.
        </p>
      </div>
    </div>
  </div>

  <!-- 템플릿 선택 -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <% @templates.each do |template| %>
        <%= link_to tournament_quick_path(template_id: template[:id]), 
                    method: :post,
                    class: "block" do %>
          <div class="bg-white rounded-lg shadow-md hover:shadow-xl transition-shadow duration-300 p-6 border-2 border-transparent hover:border-blue-500">
            <div class="flex items-center justify-between mb-4">
              <span class="text-4xl"><%= template[:icon] %></span>
              <span class="bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded">
                인기
              </span>
            </div>
            
            <h3 class="text-xl font-semibold text-gray-900 mb-2">
              <%= template[:name] %>
            </h3>
            
            <p class="text-gray-600 mb-4">
              <%= template[:description] %>
            </p>
            
            <div class="space-y-2 text-sm text-gray-500">
              <div class="flex items-center">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
                최대 <%= template[:settings][:max_teams] %>팀
              </div>
              
              <div class="flex items-center">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                약 <%= template[:settings][:duration] %>시간 소요
              </div>
              
              <div class="flex items-center">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                참가비 <%= number_to_currency(template[:settings][:entry_fee]) %>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- 커스텀 대회 만들기 -->
    <div class="mt-8">
      <%= link_to tournament_quick_path, method: :post, class: "block" do %>
        <div class="bg-white rounded-lg shadow-md hover:shadow-xl transition-shadow duration-300 p-8 border-2 border-dashed border-gray-300 hover:border-blue-500 text-center">
          <div class="text-4xl mb-4">✨</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            직접 만들기
          </h3>
          <p class="text-gray-600">
            템플릿을 사용하지 않고 처음부터 대회를 만들어보세요
          </p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- 도움말 -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="bg-blue-50 rounded-lg p-6">
      <h3 class="text-lg font-semibold text-blue-900 mb-2">
        💡 도움말
      </h3>
      <ul class="space-y-2 text-blue-800">
        <li class="flex items-start">
          <span class="mr-2">•</span>
          <span>템플릿을 선택하면 기본 설정이 자동으로 적용됩니다</span>
        </li>
        <li class="flex items-start">
          <span class="mr-2">•</span>
          <span>언제든지 세부 설정을 변경할 수 있습니다</span>
        </li>
        <li class="flex items-start">
          <span class="mr-2">•</span>
          <span>대회 생성 후에도 수정이 가능합니다</span>
        </li>
      </ul>
    </div>
  </div>
</div>