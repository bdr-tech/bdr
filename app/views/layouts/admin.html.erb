<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= yield(:title) || "BDR 관리자" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <script src="https://cdn.tailwindcss.com"></script>
  
  <!-- 반응형 텍스트 설정 -->
  <style>
    /* Tailwind 클래스에 반응형 폰트 크기 적용 */
    .text-xs { font-size: clamp(0.625rem, 1.5vw, 0.75rem) !important; }
    .text-sm { font-size: clamp(0.75rem, 2vw, 0.875rem) !important; }
    .text-base { font-size: clamp(0.875rem, 2.5vw, 1rem) !important; }
    .text-lg { font-size: clamp(1rem, 3vw, 1.125rem) !important; }
    .text-xl { font-size: clamp(1.125rem, 3.5vw, 1.25rem) !important; }
    .text-2xl { font-size: clamp(1.25rem, 4vw, 1.5rem) !important; }
    .text-3xl { font-size: clamp(1.5rem, 5vw, 1.875rem) !important; }
    .text-4xl { font-size: clamp(1.875rem, 6vw, 2.25rem) !important; }
    .text-5xl { font-size: clamp(2.25rem, 7vw, 3rem) !important; }
  </style>
</head>
<body class="bg-gray-100">
  <%= render 'shared/admin_navigation' %>
  
  <div class="container mx-auto p-responsive">
    <!-- 알림 메시지 -->
    <% if flash[:notice] %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
        <%= flash[:notice] %>
      </div>
    <% end %>
    
    <% if flash[:alert] %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <%= flash[:alert] %>
      </div>
    <% end %>
    
    <%= yield %>
  </div>
</body>
</html>