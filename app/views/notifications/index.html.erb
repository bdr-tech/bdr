<div class="min-h-screen bg-gray-50 py-8">
  <div class="max-w-4xl mx-auto px-4">
    <!-- 헤더 -->
    <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-bold text-gray-800">🔔 알림</h1>
          <p class="text-gray-600 mt-1">
            읽지 않은 알림 <span class="font-semibold text-orange-600"><%= current_user.unread_notifications.count %></span>개
          </p>
        </div>
        
        <div class="flex space-x-2">
          <% if current_user.unread_notifications.any? %>
            <%= link_to "모두 읽음 표시", mark_all_as_read_notifications_path, 
                  method: :post,
                  class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg text-sm transition duration-300" %>
          <% end %>
          
          <% if current_user.notifications.read.any? %>
            <%= link_to "읽은 알림 삭제", destroy_all_read_notifications_path, 
                  method: :delete,
                  data: { confirm: "읽은 알림을 모두 삭제하시겠습니까?" },
                  class: "bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded-lg text-sm transition duration-300" %>
          <% end %>
        </div>
      </div>
    </div>
    
    <!-- 필터 -->
    <div class="bg-white rounded-lg shadow p-4 mb-6">
      <div class="flex flex-wrap gap-2">
        <%= link_to "전체", notifications_path, 
              class: "px-4 py-2 rounded-lg text-sm font-medium transition duration-300 #{params[:status].blank? ? 'bg-orange-500 text-white' : 'bg-gray-200 text-gray-700 hover:bg-gray-300'}" %>
        
        <%= link_to "읽지 않음", notifications_path(status: 'unread'), 
              class: "px-4 py-2 rounded-lg text-sm font-medium transition duration-300 #{params[:status] == 'unread' ? 'bg-orange-500 text-white' : 'bg-gray-200 text-gray-700 hover:bg-gray-300'}" %>
        
        <%= link_to "읽음", notifications_path(status: 'read'), 
              class: "px-4 py-2 rounded-lg text-sm font-medium transition duration-300 #{params[:status] == 'read' ? 'bg-orange-500 text-white' : 'bg-gray-200 text-gray-700 hover:bg-gray-300'}" %>
      </div>
    </div>
    
    <!-- 알림 목록 -->
    <% if @notifications.any? %>
      <div class="space-y-2">
        <% @notifications.each do |notification| %>
          <div class="notification-item bg-white rounded-lg shadow hover:shadow-md transition duration-300 <%= notification.unread? ? 'border-l-4 border-orange-500' : '' %>">
            <div class="p-4">
              <div class="flex items-start">
                <!-- 아이콘 -->
                <div class="flex-shrink-0 mr-3">
                  <div class="w-10 h-10 rounded-full flex items-center justify-center <%= notification.color_class %>">
                    <span class="text-xl"><%= notification.icon %></span>
                  </div>
                </div>
                
                <!-- 내용 -->
                <div class="flex-1 min-w-0">
                  <div class="flex items-start justify-between">
                    <div class="flex-1">
                      <h3 class="text-sm font-semibold text-gray-900 <%= notification.unread? ? '' : 'opacity-75' %>">
                        <%= notification.title %>
                      </h3>
                      <p class="text-sm text-gray-600 mt-1 <%= notification.unread? ? '' : 'opacity-75' %>">
                        <%= notification.message %>
                      </p>
                      <p class="text-xs text-gray-500 mt-2">
                        <%= time_ago_in_words(notification.created_at) %>전
                      </p>
                    </div>
                    
                    <!-- 액션 버튼 -->
                    <div class="flex items-center ml-4 space-x-2">
                      <% if notification.action_url %>
                        <%= link_to notification_path(notification), 
                              class: "text-blue-600 hover:text-blue-800 text-sm font-medium" do %>
                          보기 →
                        <% end %>
                      <% end %>
                      
                      <div class="relative group">
                        <button class="text-gray-400 hover:text-gray-600">
                          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z"></path>
                          </svg>
                        </button>
                        
                        <div class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-10">
                          <div class="py-1">
                            <% if notification.unread? %>
                              <%= link_to "읽음 표시", mark_as_read_notification_path(notification), 
                                    method: :post,
                                    class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" %>
                            <% else %>
                              <%= link_to "읽지 않음 표시", mark_as_unread_notification_path(notification), 
                                    method: :post,
                                    class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" %>
                            <% end %>
                            
                            <%= link_to "삭제", notification_path(notification), 
                                  method: :delete,
                                  data: { confirm: "이 알림을 삭제하시겠습니까?" },
                                  class: "block px-4 py-2 text-sm text-red-600 hover:bg-gray-100" %>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      
      <!-- 페이지네이션 (필요시) -->
      <% if @notifications.respond_to?(:total_pages) && @notifications.total_pages > 1 %>
        <div class="mt-6 flex justify-center">
          <%= paginate @notifications %>
        </div>
      <% end %>
    <% else %>
      <!-- 알림이 없을 때 -->
      <div class="bg-white rounded-lg shadow-lg p-12 text-center">
        <div class="text-6xl mb-4">🔔</div>
        <h3 class="text-xl font-semibold text-gray-800 mb-2">알림이 없습니다</h3>
        <p class="text-gray-600">새로운 소식이 있으면 여기에 표시됩니다.</p>
      </div>
    <% end %>
  </div>
</div>

<style>
  .notification-item {
    transition: all 0.3s ease;
  }
  
  .notification-item:hover {
    transform: translateX(4px);
  }
</style>